@model EmployeeManagementSystem.Models.Employee

@{
    ViewData["Title"] = "Create Employee";
}

<!-- Create Employee Card -->
<div class="employee-card">
    <!-- Employee Data Header -->
    <div class="employee-card-header">
        Create Employee
    </div>

    <!-- Card Body -->
    <div class="employee-card-body">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger mb-4" role="alert"></div>

            <div class="row">
                <!-- SESA ID -->
                <div class="col-md-6 mb-4">
                    <label asp-for="SesaId" class="form-label">SESA ID</label>
                    <input asp-for="SesaId" class="form-control form-input" placeholder="Enter SESA ID" />
                    <span asp-validation-for="SesaId" class="validation-error"></span>
                </div>

                <!-- Full Name -->
                <div class="col-md-6 mb-4">
                    <label asp-for="Name" class="form-label">Full Name</label>
                    <input asp-for="Name" class="form-control form-input" placeholder="Enter full name" />
                    <span asp-validation-for="Name" class="validation-error"></span>
                </div>
            </div>

            <div class="row">
                <!-- Gender -->
                <div class="col-md-6 mb-4">
                    <label asp-for="GenderId" class="form-label">Gender</label>
                    <select asp-for="GenderId" class="form-select form-input" asp-items="ViewBag.GenderId">
                        <option value="">-- Select Gender --</option>
                    </select>
                    <span asp-validation-for="GenderId" class="validation-error"></span>
                </div>

                <!-- Department -->
                <div class="col-md-6 mb-4">
                    <label asp-for="DepartmentId" class="form-label">Department</label>
                    <select asp-for="DepartmentId" class="form-select form-input" asp-items="ViewBag.DepartmentId">
                        <option value="">-- Select Department --</option>
                    </select>
                    <span asp-validation-for="DepartmentId" class="validation-error"></span>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="form-actions">
                <a asp-action="Index" class="btn-cancel">Cancel</a>
                <input type="submit" value="Save Employee" class="btn-save" />
            </div>
        </form>
    </div>
</div>

<style>
    .form-label {
        font-size: 14px;
        font-weight: 600;
        color: #333;
        margin-bottom: 8px;
        display: block;
    }

    .form-input {
        padding: 12px 16px;
        border: 1px solid #ddd;
        border-radius: 6px;
        font-size: 14px;
        width: 100%;
        transition: border-color 0.2s, box-shadow 0.2s;
    }

    .form-input:focus {
        outline: none;
        border-color: #28a745;
        box-shadow: 0 0 0 3px rgba(40, 167, 69, 0.1);
    }

    .form-actions {
        display: flex;
        gap: 12px;
        justify-content: flex-end;
        margin-top: 32px;
        padding-top: 24px;
        border-top: 1px solid #e9ecef;
    }

    .btn-save {
        background-color: #28a745;
        border: none;
        color: white;
        padding: 12px 24px;
        border-radius: 6px;
        font-size: 14px;
        font-weight: 600;
        cursor: pointer;
        text-decoration: none;
        transition: background-color 0.2s;
    }

    .btn-save:hover {
        background-color: #218838;
    }

    .btn-cancel {
        background-color: #6c757d;
        border: none;
        color: white;
        padding: 12px 24px;
        border-radius: 6px;
        font-size: 14px;
        font-weight: 600;
        text-decoration: none;
        transition: background-color 0.2s;
    }

    .btn-cancel:hover {
        background-color: #5a6268;
        color: white;
    }

    .validation-error {
        color: #dc3545;
        font-size: 12px;
        margin-top: 4px;
        display: block;
    }

    .alert {
        padding: 12px 16px;
        border-radius: 6px;
        border: 1px solid transparent;
    }

    .alert-danger {
        background-color: #f8d7da;
        border-color: #f5c6cb;
        color: #721c24;
    }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}